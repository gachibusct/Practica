#include <iostream>
#include <cmath>
using namespace std;
int main()
{
 float a, b, c, d;
 cout << "Введите 2 числа" << endl;
cin >> a >> b;
if(a<b){
    c=pow(abs(a*a-b*b),1/2);
}
else {
    c=1-2*cos(a)*sin(a);
}
if(c<b){
    d=pow(c+a*a*b,1/3);
}
else{
    if(c==b){
        d=1-log10(c)+cos(a*a*b);
    }else{
    d=1/cos(c*a);
}
}
cout << " a=" << a << " b=" << b << " с=" << c << " d=" << d << endl;
 return 0;

}

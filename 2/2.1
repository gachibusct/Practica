#include "pch.h"
#include <iostream>
#include <cmath>
using namespace std;
int main()
{
setlocale(0, "");
    float a , b , c , d;
    cout << "Write a and b" << endl;
 cin >> a >> b;
 if(a>=b){
     c=pow(a-b,1/3.0);
}
 else {
 c=a*a+(a-b)/sin(a*b);
 }
 if(c<b){
     d=((c+a)/-b)+pow(sin(a)*sin(a)-cos(c),1/2.0);
 }
 else {
     if(c==b){
         d=b*b + tan(c*a);
     }else{
         d=b*b*b +c*a*a;
     }
 }
 cout << " a=" << a << " b=" << b << " c=" << c << " d=" << d << endl;
 return 0;
}

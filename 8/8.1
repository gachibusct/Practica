#include "pch.h"
#include <iostream>
#include <fstream>
#include <string>
using namespace std;
int main() {
setlocale(0, "");
    ofstream creating_file;
    ifstream reading_file;
    reading_file.open("myfileforwork.txt");
    if (reading_file.is_open()){
        string a;
        while(!reading_file.eof()){
            reading_file >> a;
            cout << a << ' ';
        }
        reading_file.close();
    }else{
        cout << "Файл не был найден.Еще раз.";
        creating_file.open("myfileforwork.txt",ios_base::trunc);
        creating_file.close();
    }
    return 0;
}
